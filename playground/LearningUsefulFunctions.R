v1 <- c(1.1, -7.1, 5.4, -2.7)
v2 <- c(-3.6, 4.1, 5.8, -8.0)

#abs(x) = positive value of all elements
#round(x) = rounds vector to whole numbers
#sum(x) = computes sum of all elements
#mean(x) = average of given vector/matrix